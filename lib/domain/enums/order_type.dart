/// Define os tipos de pedido disponíveis no sistema.
enum OrderType {

  /// Representa o pedido padrão (Bitolado).  
  standard,

  /// Representa o pedido de exportação.
  export,

  /// Representa o pedido de todas as larguras.
  allWidths,
}